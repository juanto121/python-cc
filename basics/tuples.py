#Tuples are like lists but can't be changed after creation
pi_tuple = (3,1,4,1,5,9)
two_exp = tuple([2,4,8,16])

#Dictionaries and maps, keys can only be inmutable
a_map = {'one':1, 'two':2, 'three':3}

print(a_map['two'])
print(a_map.keys())

map_values = a_map.values()

print(map_values[1])

